apiVersion: v1
kind: Service
metadata:
  name: gpuopt-backend
  namespace: gpuopt-system
  labels:
    app.kubernetes.io/name: gpuopt-backend
spec:
  selector:
    app.kubernetes.io/name: gpuopt-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
